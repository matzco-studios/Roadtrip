
public class Weapon : GrabbableItem
{
    public override void OnLeftClick()
    {
    }

    public override void OnRightClick()
    {
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
